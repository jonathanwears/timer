import React, {useContext} from "react";
import { StartContext } from "../StartContext";

function RestartButton() {
	// const [start] = useContext(StartContext);

	// return start && <button>Restart</button>
	return <></>
};

export default RestartButton;